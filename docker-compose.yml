version: '3.1'

services:

  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_PASSWORD: example
    volumes:
      - ./data:/var/lib/postgresql/data
      - ./backups:/backups

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
